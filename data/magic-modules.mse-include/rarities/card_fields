card style:
	rarity:
		left:				{ rarity_left() + typeline_offset_left() + typeline_offset_width() }
		top:				{ rarity_top() + typeline_offset_top() }
		width:				{ rarity_size() + typeline_offset_height() }
		height:				{ rarity_size() + typeline_offset_height() }
		z index:			1200
		alignment:			middle center
		visible:			{ (not typeline_disabled()) and not rarity_disabled() }
		render style:		image
		include file:		/magic-modules.mse-include/rarities/choice_images