card style:
	transformation:
		left:				{ transform_symbol_left() + nameline_offset_left() }
		top:				{ transform_symbol_top() + nameline_offset_top() }
		width:				{ transform_symbol_width() + nameline_offset_height() }
		height:				{ if transform_symbol_disabled() then 0 else transform_symbol_height() + nameline_offset_height() }
		visible:			{ not transform_symbol_disabled() }
		popup style:		in place
		include file:		/magic.mse-game/symbols/menu_choice_images
		render style:		image
		image:				{ transform_symbol_image(field: 1, face: 1) }
		z index:			1200