card style:
	casting cost:
		right:				{ casting_cost_right() + nameline_offset_left() + nameline_offset_width() }
		top:				{ casting_cost_top() + nameline_offset_top() }
		width:				{ casting_cost_width() }
		height:				{ casting_cost_height() + nameline_offset_height() }
		alignment:			middle right shrink-overflow
		visible:			{ (not nameline_disabled()) and not casting_cost_disabled() }
		always symbol:		true
		font:
			name:				MPlantin
			italic name:		MPlantin-Italic
			size:				{ name_font_size() }
			color:				black
		symbol font:
			name:				{ styling.casting_cost_mana_symbols or else "magic-mana-large" }
			size:				{ name_font_size() }
		z index:			1220